# 製品資料の索引と確認事項

`input/10-bcm-installer-manual/` に保管した製品PDFを、検証時に参照するための索引です。以下の製品仕様は資料に記載された内容の要約であり、本環境での動作確認結果ではありません。実環境の構成は [環境構成](environment.md)、合否・進捗は [検証状況](validation.md) を参照してください。

## 資料の版と扱い

- 確認日: 2026-09-22
- 全9冊の表紙: **NVIDIA Base Command Manager 11 / Revision `47eff3c` / 2026-09-21**
- ページ番号は、表紙を1ページとする **PDFページ番号**です。以下で本文を参照した箇所は、紙面のページ番号とも一致しています。
- `input/` はGit管理対象外です。PDFの所在はリポジトリルートからの相対パスで記載し、本文やPDF全体の複製は `docs/` に置きません。
- この版の説明・画面例と、実際のISOやインストール済みパッケージの動作が一致するかは、構築時に確認します。資料の版を実環境のインストール済みバージョンとして扱いません。

## 製品PDF一覧

以下のファイルはすべて `input/10-bcm-installer-manual/` 配下です。「表紙・目次」の資料は用途と参照先を整理した段階で、本文の確認は今後行います。

| ファイル | 用途 | 今回の確認範囲 |
|---|---|---|
| `installation-manual.pdf` | ヘッドノード導入、初回起動、通常ノードの起動、ライセンス | 表紙・目次と、後述の導入・構成・ライセンス関連本文 |
| `containerization-manual.pdf` | Kubernetes構築、ネットワーク、ストレージ、Operator | 表紙・目次と、後述のKubernetes関連本文 |
| `admin-manual.pdf` | BCMの管理操作、ソフトウェアイメージ、ノード展開、ディスク構成 | 表紙・目次と、[PXE 手順](bcm-pxe-provisioning.md#根拠と適用範囲) に示すディスク設定・識別・インストールモード・ログの本文 |
| `user-manual.pdf` | クラスタへのログイン、環境モジュール、MPI、ジョブ実行 | 表紙・目次。利用者向け操作が必要になった段階で参照 |
| `developer-manual.pdf` | BCM Python API、監視データの収集・アクション | 表紙・目次。自動化や監視拡張が必要になった段階で参照 |
| `upgrade-manual.pdf` | BCMの並行移行・インプレース更新、関連アプリケーションの更新 | 表紙・目次。更新検証を行う段階で参照 |
| `cloudbursting-manual.pdf` | パブリッククラウドへのクラスタ拡張 | 表紙・目次。現在の単一KVMホストでの検証範囲外 |
| `edge-manual.pdf` | エッジサイト、ノード、エッジ用ISOの管理 | 表紙・目次。現在の検証範囲外 |
| `nvidia-mission-control-manual.pdf` | Mission Controlの機能、ラック・電源等の管理 | 表紙・目次。現在のGPUなし仮想環境の主な参照対象外 |

## BCM導入・OS展開の根拠

参照資料: `input/10-bcm-installer-manual/installation-manual.pdf`

| 項目 | 章・PDFページ | 資料で確認できた内容と本検証での扱い |
|---|---|---|
| OSとアーキテクチャ | §2.1.1 p.19、§2.1.2 p.20 | Ubuntu 24.04とx86_64が記載されている。本環境で実際に導入したOS・BCMの版は別途記録する |
| 通常ノードのOS展開 | §1.3 pp.10-11、§2.2 p.20、第3章 p.23 | ヘッドノードからネットワーク起動し、ソフトウェアイメージを展開する。既存OS・データを上書きし得るため、OS用ディスクとTopoLVM用追加ディスクの対象範囲を構築前に確定する |
| 最小構成の目安 | §3.1 pp.23-24 | ヘッド1台・通常ノード2台を対象とした最小要件。7台構成全体の容量設計や性能保証としては用いない |
| 仮想環境への導入 | §3.3 p.25 | OS未導入のKVM等の仮想マシンも導入対象として説明されている。仮想化固有の追加調整があり得る。本環境のQ35・UEFI・virtioの組み合わせの動作は実測で確認する |
| 内部・外部ネットワーク | §3.3.9 pp.34-35 | Type 1は通常ノードを内部ネットワークに置き、ヘッドノードがDHCP・PXEと外部へのルーティングを担う。本環境で予定するヘッドノード経由の外部接続を検討する際の参照先 |
| 初回起動後の確認 | §1.2 p.10、§3.3.21 p.50 | ライセンス属性確認、パッケージ更新、追加構成を案内している。インストーラー画面を確認しただけで、これらの完了とは判定しない |

### ライセンスと実バージョンの確認

同資料の第4章 p.57 は、ISOに同梱される標準評価ライセンスの上限を2ノードと説明しています。ヘッド1台と通常ノード6台の本検証では、実際に適用したライセンスのノード数と有効期限を確認する必要があります。この記載から、本環境に適用されたライセンスの種類や不足を断定はしません。

参照箇所は §4.1 pp.58-59（属性表示）、§4.2 pp.59-61（検証）、§4.2.3 pp.61-62（BCMソフトウェアの版）です。ヘッドノードもライセンスのノード数に含まれます。資料では、ライセンスに表示されるバージョンとBCMソフトウェアのバージョンは別物であることも説明されています。

資料に記載された確認手段は以下です。BCM の導入後、管理ツールを利用できるヘッドノード上で確認するための参照情報で、本環境での実行・確認済みログではありません。

| 確認対象 | 確認手段 | 根拠 |
|---|---|---|
| ライセンス属性 | `cmsh -c "main licenseinfo"` | §1.2 p.10、§4.1.2 p.59 |
| CMDaemonが起動しない場合も含めたライセンス情報 | `verify-license info` | §4.2.1、§4.2.2 p.60 |
| ライセンスの有効性 | `verify-license verify` | §4.2.2 p.61 |
| BCMソフトウェアの版 | `cmsh` の `main` モードで `versioninfo` | §4.2.3 pp.61-62 |

### ライセンスの取得と登録

2026-09-22 に [NVIDIA のライセンス案内](https://docs.nvidia.com/base-command-manager/bcm-licensing-and-support/)（ページ更新日 2026-09-04）を確認。現在の無償ライセンスはノード数無制限、各システム最大 8 アクセラレータで、CPU のみのクラスタにも利用できる。組織の自社利用が対象で、Enterprise Support は含まない。既定の期間は 1 年、更新申請が可能。ISO 同梱の 2 ノード制限と区別する。本利用者への権利付与・承認を確認したものではない。

プロダクトキー未取得の場合は [NVIDIA Enterprise Account Registration](https://enterpriseproductregistration.nvidia.com/?LicType=Eval&ProductFamily=BCM) から申請し、権利付与メール受領後に [NVIDIA Licensing Portal](https://ui.licensing.nvidia.com/) の `Entitlements` → `Base Command Manager` → 該当行の `Actions` → `Generate key` でキーを取得する。公式案内のメール受領目安は通常 2 営業日。企業メール・会社名の照合条件も案内に従う。

登録コマンドは **BCM ヘッドノードの root の OS シェル**で実行する。cmsh 内では `request-license` は認識されない。2026-09-22 の利用者による登録操作、実際の入力順、成功表示、証明書更新、登録後の照会は [BCM ライセンス登録手順と実施記録](bcm-licensing.md) にまとめた。ライセンスの現在値・進捗は [検証状況](validation.md) を参照する。

手順の根拠: `input/10-bcm-installer-manual/installation-manual.pdf`、BCM 11 / Revision `47eff3c` / 2026-09-21、§4.3.2–4.3.4 pp.62–66、§4.3.7 pp.67–68（PDF 通し番号と紙面番号が一致）。一般的な無償ライセンスの説明と、今回発行された種別・上限・期間を区別し、実際の表示を優先して記録する。

## Kubernetes構築の根拠

参照資料: `input/10-bcm-installer-manual/containerization-manual.pdf`

Ubuntu 24.04 を前提とした BCM 11.34 / 11.33 の比較は [BCM / Kubernetes バージョン対応](bcm-kubernetes-versions.md) を参照してください。2026-09-22 に第4章 pp.27–29・付録 B p.189 と公開公式資料を確認し、導入例・現行マニュアルの候補・実機での確認事項を整理しています。公開 PDF は Revision `1e6a090` / 2026-09-22 で、手元の Revision `47eff3c` とは区別しています。

構築用の `cm-kubernetes-setup` は BCM ヘッドノード上で、BCM の導入・対象ノードの準備後に利用するツールです。以下は資料の索引であり、そのまま実行する構築手順ではありません。

| 項目 | 章・PDFページ | 資料で確認できた内容と本検証での扱い |
|---|---|---|
| 導入方式と版 | 第4章 p.27、§4.2 pp.28-29 | BCMはKubernetesの導入にkubeadmを使用する。導入可能な版は `cm-kubernetes-setup --list-versions` と実パッケージで確認し、本環境の採用版を決める |
| コントロールプレーン・etcd | §4.1 p.28 | etcdは奇数台、高可用性を目指す場合はetcd 3台以上・コントロールプレーン2台以上を推奨。BCMではetcdをKubernetes外で動作させると説明されている（p.27）。本検証のCP3台・worker3台に加え、etcdの配置を明示する必要がある |
| ノードの資源 | §4.1.1 p.28 | Kubernetes導入のノード当たり最小要件はメモリ16 GB・CPU 2コア。実VM割り当てとホストの稼働余力を分けて確認する |
| 設定ツール | §4.2 p.28、§4.2.8 pp.40-51 | `cm-kubernetes-setup` によるTUI構築を説明。実行ログの場所は `/var/log/cm-kubernetes-setup.log`（§4.2.1 p.30） |
| ノード割り当て | §4.2.8 pp.43-44 | ヘッドノードがCP・etcdとして初期選択される。専用CP3台を使う本検証では、既定値をそのまま採用せず、CP・worker・etcdの選択を構成と照合する |
| Pod・Serviceネットワーク | §4.2.1 p.30 | Pod CIDR・Service CIDRを設定する。既存ネットワークとの重複を避けた割り当てを確定する |
| 基本CNI | §4.2.2 pp.31-32、§4.2.8 p.44 | ウィザードのCNI選択肢としてCalicoとFlannelを説明。Multus用セカンダリネットワークと合わせ、基本CNIの採用方針を別途決定する |
| GPU関連 | §4.2.8 pp.46、48-49 | NVIDIA GPU OperatorやNVIDIA関連パッケージの選択を説明。本検証のGPUなし構成では、選択内容をその前提と照合する |
| ストレージ | §4.2.8 p.50、§4.20 p.94 | ウィザードはlocal-path StorageClassを設定でき、その保存先を全ノードから利用可能な共有ファイルシステムとする説明。各workerの追加ディスクを使うTopoLVM要件とは区別する |
| 初期動作確認 | §4.2.9 p.51 | `kubectl cluster-info`、`kubectl get nodes` 等を用いた確認例。掲載結果は製品資料の例であり、本環境の検証結果ではない |

### Multus・TopoLVMの追加調査

今回確認したInstallation ManualとContainerization Manualのテキスト検索では、`Multus` と `TopoLVM` の記載を確認できませんでした。これは両製品の対応可否を判定したものではありません。

Multusの導入方法・基本CNIとの組み合わせ・セカンダリNIC設定、TopoLVMの導入方法・追加ディスクとVGの構成・StorageClassは、追加の製品資料や実行ログに基づいて文書化します。BCMのOS再展開・再起動時にこれらの設定とデータを保持する方法も未確認です。判断と実測結果は [検証要件](requirements.md) および [検証状況](validation.md) に反映します。

## OS展開の詳細化時に参照する章

`input/10-bcm-installer-manual/admin-manual.pdf` の参照箇所です。2026-09-22 にディスク設定・PXE・識別・インストールモード・ログの本文を確認し、[本環境向け PXE 手順](bcm-pxe-provisioning.md) に具体化しました。§9.4 のパッケージ管理は引き続き参照候補です。実機で設定変更や展開を実行した結果ではありません。

| 確認したい事項 | 章・開始PDFページ |
|---|---|
| ノード／カテゴリのディスクレイアウト | §3.13 p.166 |
| PXE起動 | §5.1.1 p.239 |
| ノード識別と構成選択 | §5.4.2 p.264 |
| インストールモード | §5.4.4 p.276 |
| パーティション確認とイメージ同期 | §5.4.6 p.281、§5.4.7 p.282 |
| PXE起動失敗とログ | §5.8 p.309、§5.8.2 p.313、§5.8.3 p.314 |
| ソフトウェアイメージ内のパッケージ管理 | §9.4 p.535 |

追加資料を読んだ際は、版・章・PDFページ・本検証への適用判断を追記します。構築ログから確認した事実は、その日時と入力ファイルを添えて対応する構築記録や検証状況に記載します。
