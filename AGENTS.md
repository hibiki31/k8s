# リポジトリ作業方針

## 目的と対象

このリポジトリでは NVIDIA Base Command Manager（BCM）を使った Kubernetes クラスタの検証を、KVM / QEMU・libvirt の仮想環境で行います。BCM による PXE・OS 展開、コントロールプレーン 3 台・ワーカー 3 台、Multus、TopoLVM が対象です。

本リポジトリは **Public 公開を前提**とします。コミットする情報は、不特定多数が取得・保存・再利用できるものとして判断してください。**毎回のコミット前に、下記の公開情報チェックを必ず実施し、問題が解消するまでコミットしません。** 初回コミットや amend も対象です。

作業開始時に `README.md` と、作業に関係する `docs/` の文書を確認してください。ユーザーの明示的な指示を優先します。

## ファイルの役割

| 場所 | 役割 |
|---|---|
| `README.md` | プロジェクト概要、`docs/` の索引、資料更新の入口 |
| `docs/requirements.md` | 検証要件、対象範囲、確認条件 |
| `docs/environment.md` | 環境構成、共通設定、未確定の設定事項 |
| `docs/vm-list.md` | VM ごとのリソース割り当て |
| `docs/references.md` | 製品資料の一覧、版、参照する章・ページ |
| `docs/bcm-installation.md` | BCM 構築の設定・手順・証跡に基づく記録 |
| `docs/validation.md` | 準備・構築・検証の進捗と根拠へのリンク |
| `input/` | 製品ドキュメント、構築ログ、画面などの一次資料。Git 管理外 |

新しい構築・検証テーマの文書は `docs/` に追加し、README の索引から参照できるようにします。環境情報や進捗を複数文書に重複して管理せず、上表の文書に集約して他の文書からリンクします。

## 一次資料の扱い

- `input/` はリポジトリルートの `.gitignore` にある `/input/` で全体を除外します。原本を強制追加したり、Git 管理する場所へ移して除外を回避したりしません。
- `input/` の資料を読んでから、必要な内容を `docs/` に整理します。原本のログや製品マニュアルをそのまま大量転載せず、環境に必要な設定・手順・結果を記述します。
- 参照元はリポジトリ相対パスに加え、資料のバージョン、章・節とページ、またはログ・画面の日時を記録します。PDF の通しページと紙面のページ番号が異なる場合は区別します。
- `input/` のパスは、clone 後には存在しない可能性があるためインラインコードで示します。Git 管理する文書同士は相対 Markdown リンクで結びます。
- 製品資料に書かれた一般仕様と、この環境で観測した事実を区別します。別バージョンの資料を使う場合は適用可否を確認し、不明な点を明記します。
- 一次資料がない、読めない、資料同士で矛盾する場合はその状態を記録します。推測で空欄を埋めたり、既存の実測記録を一般的な推奨値に置き換えたりしません。
- パスワード、トークン、秘密鍵、ライセンスキーなどの実値は Git 管理文書へ転記せず、下記の公開基準に従います。「検証用」「非機密」「閉域環境で使用」という指定だけでは、インターネットへの公開許可とは扱いません。既存の `docs/environment.md` の検証用パスワードにもこの基準を適用します。

## Public 公開に向けた情報の判断基準

必要性、認証・アクセスへの利用可否、個人・組織・実環境の特定につながるか、公開・再配布の権限があるかの観点で判断します。単一の値だけでなく、複数の情報を組み合わせた場合も確認してください。

| 分類 | 判断基準と例 | 扱い |
|---|---|---|
| 公開可能な検証情報 | 自作の手順・検証結果、一般的な製品名・公開仕様、VM の役割・リソース量、公開用に設定した架空の値。認証情報や第三者の非公開情報を含まないもの | 検証の理解・再現に必要な範囲で記載する |
| 認証情報・秘密情報 | 実際に使用するパスワード、API トークン、秘密鍵、セッション Cookie、Authorization ヘッダー、接続・署名付き URL、ライセンスキー、認証情報入り kubeconfig・Kubernetes Secret・レジストリ設定、復元可能な符号化値やパスワードハッシュ | 公開不可。実値を削除し、`<REDACTED>` や `<PASSWORD>` 等のプレースホルダーに置き換える。Base64 化や暗号化だけで公開可能とは判断しない |
| 個人・組織・契約に関する情報 | 個人名、メールアドレス、ユーザー名、顧客・所属組織の非公開情報、契約・購入情報、アカウント ID、ライセンスの識別情報 | 公開済みまたは公開許可のある情報で、記載の必要性がある場合だけ使用する。それ以外は匿名化・削除する |
| 実環境の識別・接続情報 | 実 IP アドレス、ドメイン、接続先 URL、ホスト名、MAC アドレス、シリアル番号、UUID、個人名を含む絶対パス、ネットワーク構成 | 検証専用で公開可能と確認でき、記載が必要なものだけ使用する。プライベート IP や内部ホスト名も自動的に安全とは扱わない。未確認なら例示値やリポジトリ相対パスに置き換える |
| 第三者の著作物・非公開資料 | 製品 PDF、ISO、ソフトウェア、サポート回答、顧客資料、転載した図表・画面 | 公開・再配布の許可条件を確認できない原本や転載物は含めない。公開 Web 上で閲覧できることだけを再配布の根拠にしない。原本は `input/` に置き、`docs/` には公開可能な範囲の独自の説明・要約と参照元を記載する。非公開情報は要約しても公開可能にはならず、出典表示だけでは公開許可の代わりにならない |
| ログ・画像・バイナリ・生成物 | コマンド出力、スクリーンショット、PDF、アーカイブ、設定のエクスポート、一時ファイル | 上記の各基準を内容全体に適用する。本文だけでなく URL のクエリ、画面の周辺部、メタデータ、埋め込みファイルも確認する。内容を確認できないものはコミット対象から外す |

「公開可能」とする根拠がない情報を、安全と推測して残しません。値の全部がなくても技術的な説明が成立する場合は、削除・匿名化を優先します。マスキング後も元の情報を復元・推測できないことを確認します。置き換えた値は例示値・匿名化済みと明記し、実測値として扱いません。

検証用パスワードの実値を例示として残せるのは、**インターネットでの公開が明示的に許可され、実際の認証に使用・再利用されない公開用のダミー値であることを確認できた場合だけ**です。既存の `docs/environment.md` の値は、従来の非機密指定だけでこの条件を満たしたと判断せず、コミット前に条件を確認するかプレースホルダーへ置き換えてください。

## コミット前の公開情報チェック（必須）

1. `git status --short --untracked-files=all` で変更・未追跡ファイルを確認し、今回コミットするファイルを明確にする。新規ファイル、隠しファイル、一時生成物も対象にする。
2. ステージ後に `git diff --cached --name-status`、`git diff --cached --check`、`git diff --cached` を確認する。**作業ツリーだけでなく、実際にコミットされるステージ済みの内容を確認する。** 新規ファイルは全文、変更ファイルは差分と関連する文脈を読む。画像・バイナリは実際にステージされた版の内容を確認し、テキスト差分に出ないことを確認省略の理由にしない。
3. 上記の判断基準で、認証情報、個人・組織情報、接続情報、第三者資料を確認する。`password`、`token`、`secret`、`Authorization`、`PRIVATE KEY`、URL 等の検索も補助に使うが、検索やスキャンで検出されないことだけで安全と判定しない。ファイル名、コミットメッセージ、著者名・メールアドレス等のコミット情報にも、意図しない個人情報や秘密情報がないことを確認する。調査・完了報告に問題の実値を再掲しない。
4. `git ls-files -- input` で `input/` 配下が追跡・ステージされていないことを確認し、`git check-ignore -v -- input/<対象ファイル>` で除外を確認する。`.gitignore` は既に追跡済みのファイルや過去のコミットには効かないため、除外設定の存在だけで判断しない。原本が別のパスへコピーされていないかも確認する。
5. 問題があれば削除・匿名化・対象からの除外を行い、再ステージして同じ確認をやり直す。作業ツリー上の修正だけで解消したと判断しない。公開可否が不明で、削除・匿名化では依頼された成果を満たせない情報は、該当箇所と必要な判断をユーザーへ伝え、解決するまでコミット対象に含めない。
6. 初回の Public 公開前には、コミット対象の全ファイルに加えて、**公開するブランチ・タグの過去のコミット**にも問題情報が含まれないことを確認する。履歴がある場合、現在のファイルから削除しただけで公開可能と判断しない。履歴に認証情報が見つかった場合は公開を止め、履歴からの除去と認証情報の失効・再発行の必要性を報告する。
7. コミット時の完了報告に、確認した範囲、チェック結果、削除・匿名化の有無、未解決事項の有無を簡潔に記載する。追加の変更・再ステージを行った場合は、コミット直前に再確認する。

## ドキュメントの記述・更新

- 本文は日本語、ファイル名は内容が分かる英小文字とハイフンを基本とします。製品名、コマンド、実際の設定キーは原表記を保ちます。
- 要件・計画・画面上の入力値・実行済み操作・確認結果・未確認事項を区別します。インストーラーの設定画面だけで、インストール完了や設定反映を断定しません。
- 作業記録には実施日、対象、バージョン、前提、操作・設定、期待結果、実際の結果、証跡、残課題を必要な範囲で記載します。資料にない情報は「未記録」「未確認」「未定」などと明示します。
- コマンドを載せる場合は実行場所（物理ホスト、BCM、対象ノード等）と前提を示し、未実行の例を実行済みログとして扱いません。ディスク初期化等は対象の識別と影響を記載します。
- 既存の確認結果を引き継ぐ場合は、その情報源と今回再検証していないことを明記します。失敗や制約も残します。
- 構成変更時は環境構成・VM 一覧・関連要件を、進捗変更時は検証状況を更新します。新規文書の追加・改名・移動時は README と関連リンクも更新します。
- README に詳細な手順や作業ログを積み上げず、概要と索引を維持します。
- 文書整備のために、VM・クラスタ・ネットワーク・ディスクを変更する必要はありません。実環境への操作は依頼された作業の範囲で行います。

## 更新後の確認

1. Markdown の見出し、表、コードブロック、相対リンクを確認する。
2. 台数・リソースの合計や、設定値・バージョン・進捗に矛盾がないことを確認する。
3. 参照した一次資料と記述が一致し、計画や未確認事項を成功済みとしていないことを確認する。
4. `git status --short` と差分を確認し、`input/` の原本や一時的な抽出ファイルが追加対象に入っていないことを確認する。必要なら `git check-ignore -v -- input/<対象ファイル>` で除外を確認する。
5. コミットを行う場合は「コミット前の公開情報チェック（必須）」をすべて実施する。
6. 完了報告では変更した文書と、行った確認、残る未確認事項を簡潔に示す。文書の確認と実環境の検証を区別する。
